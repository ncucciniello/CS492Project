# Migration `20201106030440-migration`

This migration has been generated by ncucciniello at 11/5/2020, 10:04:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201106014616-migration..20201106030440-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -13,13 +13,14 @@
 // Define your own datamodels here and run `yarn redwood db save` to create
 // migrations for them.
 // TODO: Please remove the following example:
 model User {
-  id      Int     @id @default(autoincrement())
-  email   String  @unique
-  name    String?
-  type    String
-  trainer Int?
+  id       Int       @id @default(autoincrement())
+  email    String    @unique
+  name     String?
+  type     String
+  trainer  Int?
+  workouts Workout[]
 }
 model Workout {
   id        Int        @id @default(autoincrement())
@@ -29,9 +30,9 @@
   exercises Exercise[]
 }
 model Exercise {
-  id             Int      @id @default(autoincrement())
+  id             Int          @id @default(autoincrement())
   workoutId      Int
   weight         Int
   repsAssigned   Int
   repsComplete   Int?
@@ -42,8 +43,9 @@
   exerciseType   ExerciseType @relation(fields: [exerciseTypeId], references: [id])
 }
 model ExerciseType {
-  id           Int      @id @default(autoincrement())
-  name         String
-  description  String?
+  id          Int        @id @default(autoincrement())
+  name        String
+  description String?
+  exercises   Exercise[]
 }
```


