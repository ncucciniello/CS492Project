# Migration `20201106014616-migration`

This migration has been generated by ncucciniello at 11/5/2020, 8:46:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201105213349-migration..20201106014616-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -21,12 +21,13 @@
   trainer Int?
 }
 model Workout {
-  id     Int       @id @default(autoincrement())
-  userId Int
-  date   DateTime @default(now())
-  user   User      @relation(fields: [userId], references: [id])
+  id        Int        @id @default(autoincrement())
+  userId    Int
+  date      DateTime   @default(now())
+  user      User       @relation(fields: [userId], references: [id])
+  exercises Exercise[]
 }
 model Exercise {
   id             Int      @id @default(autoincrement())
```


